<footer id="footer">
  <% repo = GithubApi.new %>
  <p>Repo Name:
    <%= repo.call("")[:name] %>
    All Merged PR's:
    <%= repo.count %>
    Collaborators:
    <% repo.names.each do |name| %>
      <%= name %>
      Commits:
      <%= repo.call("/commits?author=#{name}").count %>
    <% end %>
  </p>
</footer>
